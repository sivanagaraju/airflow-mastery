# ============================================================
# Python Dependencies for Airflow
# ============================================================
# These packages are installed in the custom Dockerfile.
# Pin versions for reproducibility.
# ============================================================

# Core Airflow Providers
apache-airflow-providers-http==4.12.0
apache-airflow-providers-postgres==5.12.0
apache-airflow-providers-amazon==8.28.0
apache-airflow-providers-google==10.23.0

# Data Processing (used in task code, not in DAG parsing)
pandas==2.2.0
requests==2.31.0

# Testing
pytest==8.0.0
